{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python seed_users.py && gunicorn myHospital.wsgi --bind 0.0.0.0:$PORT"
  }
}
